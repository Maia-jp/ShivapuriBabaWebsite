# Cloudflare Pages configuration.
# The site is fully static (Astro `output: "static"`), so no Workers runtime or
# adapter is required — Pages serves the built `dist/` directory directly.

name = "shivapuri-baba-website"
pages_build_output_dir = "dist"
compatibility_date = "2026-06-17"
